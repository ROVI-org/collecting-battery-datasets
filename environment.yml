name: batcollect
channels:
  - defaults
dependencies:
  - python==3.10.*

  # Etc
  - pip
  - pip:
    - mat73  # For NREL synthetic data
    - tqdm
    - selenium
    - jupyterlab
    - matplotlib
    - battery-data-toolkit>=0.3.1
